# Library configuration aggregation.
# Keep package-owned configuration definitions in their native Kconfig files;
# this menu only groups the library surface in pyconfig/menuconfig.
menu "Unified Libraries"

source "$(PROJECT_ROOT)/Libraries/Vendor/STM32/Kconfig"
source "$(PROJECT_ROOT)/Libraries/ThirdParty/Kconfig"

endmenu